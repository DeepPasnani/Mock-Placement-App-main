# ============================================================
# CampusTrack Frontend Environment Variables
# Copy this to .env and fill in values
# ============================================================

# Backend API URL
VITE_API_URL=http://localhost:5000/api

# WebSocket URL (same server, different protocol)
VITE_WS_URL=ws://localhost:5000/ws

# Google OAuth Client ID (same as backend)
# Get from https://console.cloud.google.com → APIs & Services → Credentials
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com

# App name (shown in UI)
VITE_APP_NAME=CampusTrack

# College name (shown in header/footer)
VITE_COLLEGE_NAME=Your College Name

# Dev Tools (Admin panel > Dev Tools) — local URLs for the pgAdmin and
# self-hosted Judge0 containers started via docker-compose.yml /
# infra/judge0/docker-compose.yml. Only relevant when running locally.
VITE_PGADMIN_URL=http://localhost:8467
VITE_JUDGE0_URL=http://localhost:2358
